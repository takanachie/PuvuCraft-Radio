# PuvuCraft Radio 项目设定

状态：需求基线 v1

本文档是第一版实现的产品与技术基线。业务代码、数据库结构、API 和部署文件应与本文档保持一致；如需改变标记为“已确定”的行为，应先修改本文档。

## 1. 项目目标

构建一个单机部署的多频道在线音乐电台：

- 服务器持续推进每个已启用频道的逻辑时间线；无人收听时不实际解码或转码。
- 同一频道的所有听众收听同一个直播时间点，而不是各自启动独立播放。
- 用户通过网页注册，管理员审批后才能登录和收听。
- 普通用户只能选择频道、播放或暂停、静音和调整本地音量。
- 普通用户不能切歌、改变服务器播放进度、修改歌单或上传音乐。
- 管理员通过完整后台管理用户、频道、音乐库、歌单和播放状态。
- 初期目标规模为最多约 5 个同时运行的频道和 50 名同时在线听众。

“同步”按 HLS 直播语义定义，不承诺采样级同步。正常网络条件下，同一频道客户端之间的目标进度差不超过两个 HLS 分片。

## 2. 已确定的技术栈

| 范围 | 选择 |
| --- | --- |
| 后端 | Python 3.12+、FastAPI、Uvicorn |
| 数据访问 | SQLAlchemy 2.x、Alembic |
| 数据库 | SQLite，启用 WAL 模式 |
| 转码与探测 | FFmpeg、ffprobe |
| 串流协议 | 浏览器音频 HLS；外部播放器持续 HTTP 音频流 |
| 输出音频 | AAC-LC、320 kbps、48 kHz、双声道；管理员可用 FLAC 44.1 kHz/16 bit/双声道 |
| 前端 | Vue 3、Vite、TypeScript、hls.js |
| 实时状态 | Server-Sent Events（SSE） |
| 生产入口 | Nginx、HTTPS、同源部署 |
| 进程管理 | Linux systemd |
| 部署方式 | Linux 原生服务，不以容器作为首版要求 |

首版使用单体仓库和单台服务器。FastAPI 提供 API、认证和频道控制；Vue 构建产物由 Nginx 提供；FFmpeg 由后端的频道监督器作为子进程管理。

## 3. 角色与权限

### 3.1 匿名访问者

- 可以访问登录页、注册页和首次初始化状态接口。
- 可以提交用户名、邮箱和密码进行注册。
- 不能读取频道、歌单、播放状态、HLS 清单或音频分片。

### 3.2 待审批用户

- 注册后状态为 `pending`。
- 不能登录和收听。
- 被拒绝或停用时，接口只返回通用状态，不泄露管理员备注。

### 3.3 普通用户

- 可以登录、退出和查看自己的账号信息。
- 可以查看全部可用频道、频道完整歌单、当前歌曲和播放进度。
- 可以播放或暂停本地播放器、静音、调整音量和切换频道。
- 暂停后恢复时重新回到频道直播点，不从暂停位置继续缓存播放。
- 可以在收听页复制当前频道的外部播放器 AAC 持续流连接。
- 不能拖动进度、切换服务器当前歌曲、添加或删除音乐、修改频道或查看管理接口。

### 3.4 管理员

- 拥有普通用户的全部能力。
- 可以审批、拒绝、停用、重新启用和永久删除用户，也可以将已批准的听众提升为管理员；停用、删除或角色变化后使其现有会话失效。
- 可以创建、编辑、启用、停用和删除频道。
- 可以通过所有管理员共享的公共队列上传音乐，也可以触发服务器目录扫描。
- 可以编辑歌曲标题、艺人、专辑和封面。
- 可以向歌单添加或移除歌曲、调整顺序以及设置顺序循环或随机播放。
- 可以跳到下一首，或选择歌单中的歌曲立即播放。
- 可以查看频道健康状态、FFmpeg 最近错误和最近播放记录。
- 可以在用户管理中查看和筛选当前正在收听的用户，并查看其正在接收的频道。
- 可以为自己的外部播放器连接选择 FLAC 输出；该能力不向普通用户开放。

首版只有 `admin` 和 `listener` 两种角色，不实现自定义权限组。

## 4. 账号与认证

### 4.1 注册和审批

- 注册字段为用户名、邮箱和密码。
- 用户名和规范化后的邮箱均唯一。
- 首版不发送验证邮件，也不实现邮件找回密码。
- 注册成功后账号进入待审批状态；管理员批准后方可登录。
- 管理员可执行密码重置，但系统不得通过日志或 API 返回明文密码。

### 4.2 首个管理员

- 数据库不存在管理员时，启用一次性的 `/setup` 网页向导。
- 应用首次启动时生成高强度随机初始化令牌，将其写入仅服务账号可读的文件，并在服务日志中提示令牌位置。
- 创建首个管理员必须提交该令牌；成功后立即删除令牌并永久关闭初始化入口。
- 仅判断“第一个访问者”而不校验令牌的实现不符合本设定。

### 4.3 会话安全

- 密码使用 Argon2id 哈希，不使用可逆加密。
- 浏览器使用随机、不透明、可撤销的服务端会话，不把长期 JWT 存入 localStorage。
- 会话 Cookie 必须为 `HttpOnly` 和 `SameSite=Lax`；生产环境必须启用 `Secure`。
- 修改数据的请求需要 CSRF 防护。
- 登录、注册和初始化接口需要按来源 IP 和账号标识限速。
- 用户被停用、密码被管理员重置或角色变化后，其已有会话全部失效。
- 日志不得记录密码、完整会话令牌、CSRF 令牌或初始化令牌。

### 4.4 外部播放器密钥

- 外部播放器使用独立于浏览器 Cookie 会话的播放密钥，每个账号最多只能有一个当前有效密钥。
- 功能完整实现后，新建账号在创建时生成播放密钥；升级前已经存在的账号保持未配置状态，由用户自行重新生成以首次启用。
- 用户可以重新生成播放密钥；新密钥生效后旧密钥立即失效，不允许同时保留多个密钥。
- 数据库只保存凭据创建时间和 128 bit 随机代次值，不保存完整播放密钥或其哈希。后端使用由服务秘密派生的确定性认证加密生成不透明密钥，并在连接建立时解密后与当前账号代次比对。
- 播放密钥只能在创建后 30 天内用于建立新连接；连接已成功建立后不因跨过该时间点而被主动中断。刷新有效日期会建立新的 30 天窗口。
- 待审批、已拒绝、已停用或已删除账号的播放密钥均不得用于收听。
- 播放密钥属于凭据，不得写入访问日志、应用日志、审计详情或错误响应。

## 5. 频道与播放语义

### 5.1 频道生命周期

- 每个已启用频道独立维护持久化逻辑时间线；只有浏览器 HLS 或外部播放器存在实际收听需求时才启动频道监督器和 FFmpeg。
- 最后一名听众离开后保留 30 秒空闲窗口，再停止解码器与编码器；逻辑时间线仍按墙上时间推进。
- 歌单为空、所有歌曲均不可用或 FFmpeg 连续启动失败时，频道进入 `offline` 状态并保留可诊断原因。
- 停用频道应停止对应 FFmpeg 进程并删除过期 HLS 文件。
- 后端只能存在一个拥有频道监督权的主进程；首版不得启动多个会争抢 FFmpeg 子进程的 Uvicorn worker。

频道状态至少包含：`starting`、`live`、`idle`、`degraded`、`offline` 和 `stopped`。`idle` 表示时间线正在推进但没有实际音频编码进程。

### 5.2 播放模式

- 每个频道可独立选择 `sequential` 或 `shuffle`。
- `sequential` 按歌单位置顺序循环。
- `shuffle` 使用可持久化的随机顺序；一轮内不重复，并避免跨轮连续播放同一首歌。
- 只有一首可用歌曲时允许循环播放该歌曲。
- 歌曲之间直接连续播放，首版不实现交叉淡化。

### 5.3 歌单变更

- 添加歌曲、调整非当前歌曲顺序和切换播放模式从下一次选曲开始生效。
- 从歌单移除当前歌曲或选择“立即播放”需要管理员确认，并触发受控切换。
- 删除仍被任何歌单引用的媒体文件时应拒绝操作，并显示引用它的频道。
- FFmpeg 重启或切歌造成的 HLS 时间线变化必须使用正确的不连续标记，客户端不应持续播放旧分片。

### 5.4 重启恢复

- 每个频道持久化时间线锚点、播放顺序、当前歌曲、歌曲内偏移和歌单版本。
- 应用或服务器重启后，根据经过的墙上时间和歌曲时长推算预期歌曲及偏移，并从该位置恢复。
- 恢复计算必须可以跨越多首歌曲和歌单循环。
- 若锚点歌曲已删除或文件不可用，选择当前歌单中的下一个可用项，并建立新锚点。
- 系统时间发生明显回退时不得产生负偏移；应记录警告并从最近有效状态恢复。

## 6. HLS 与 FFmpeg

### 6.1 输出要求

- 所有活动频道的浏览器输出统一编码为 AAC-LC 320 kbps、48 kHz、双声道；同一份 AAC 编码同时供 HLS 和外部播放器持续流使用。
- 默认 HLS 分片长度为 2 秒，直播清单保留 3 个分片，浏览器目标延迟约为 4–6 秒。
- 清单为直播清单，不提供点播结束标记或任意回看窗口。
- 输出采用稳定的频道标识目录，不使用用户提供的文件名组成路径。
- FFmpeg 参数由应用根据结构化配置生成，管理界面不得接受任意命令行参数。

### 6.2 进程管理

- 每个活动频道最多有一个共享 AAC 输出进程，不能按听众启动独立 AAC/HLS 编码器。管理员请求 FLAC 时允许该频道额外启动一个共享 FLAC 输出进程。
- 频道监督器捕获标准错误、退出码和最近启动时间，但需要限制日志大小。
- 异常退出采用有上限的指数退避；达到阈值后频道标记为 `degraded` 或 `offline`。
- 应用正常关闭时先停止接受管理操作，再终止 FFmpeg，并等待有限的清理时间。
- 应用启动时清理无法归属到当前运行实例的陈旧 HLS 分片。

### 6.3 客户端直播行为

- 支持原生 HLS 的浏览器优先使用原生能力，其余现代浏览器使用 hls.js。
- 浏览器自动播放受限时显示明确的“开始收听”操作，不尝试绕过浏览器策略。
- 播放器不显示可拖动的时间轴。
- 切换频道、长时间暂停后恢复、网络恢复或检测到过期清单时，播放器重新加载清单并回到直播点。
- 当前歌曲和进度由 SSE 状态提供，不以客户端本地音频时长作为权威播放状态。

## 7. HLS 访问控制

- HLS 主清单、媒体清单和每个音频分片都必须要求有效的已审批账号会话。
- 生产环境由 Nginx 提供文件，并使用 `auth_request` 调用 FastAPI 的内部流授权接口。
- HLS 与前端必须同源，使原生播放器和 hls.js 都能发送会话 Cookie。
- 内部授权接口只返回允许或拒绝，不返回用户敏感数据。
- 被停用或退出登录的用户应在短暂授权缓存到期后失去继续获取新分片的能力。
- 成功的 HLS 流授权会刷新对应用户和频道的收听活动；超过配置的短暂窗口未再请求清单或分片即视为离线，不以仅登录或仅保持 SSE 连接作为正在收听。
- 浏览器继续使用同源 Cookie 会话访问 `/hls`；不根据 User-Agent、Referer 等可伪造请求头区分浏览器和外部播放器。
- 外部播放器只能通过 `/listen/{format}/{player_key}/{channel_slug}` 建立持续 `GET` 连接；不提供外部 HLS、播放列表、`HEAD` 成功响应或其他信息接口。
- `/listen` 下缺少、格式错误、无效、被替换或已超过最晚连接时间的密钥，以及无权使用所选格式的请求，均只返回空响应体的 `404 Not Found`。不得返回 JSON、错误码、文本、重定向或具体拒绝原因。
- 密钥有效但频道不存在、已停用或音频进程无法就绪时只返回空响应体的 `503 Service Unavailable`。
- 同一个密钥代次最多保留一个持续连接。新的有效连接就绪后立即通知旧连接结束，旧连接最迟在 10 秒后被强制取消；不能为了保留旧连接而拒绝新连接。
- 每个连接最多保留约 5 秒应用层音频队列；慢连接超过该窗口后被断开，不能延长频道缓存清理周期或阻塞共享编码器。
- 外部流只设置固定站名 `PuvuFM`，永久不返回曲目、艺人、专辑、频道说明、版权或其他业务元数据。
- `/listen` 请求不得记录包含播放密钥的 URI；不得以“难以猜测的 URL”替代服务端密钥校验。
- 本地开发可以由 FastAPI 提供受保护的 HLS 文件，但生产环境不得让 Python 承担全部静态分片传输。

## 8. 音乐库

### 8.1 导入方式

- 管理员可通过网页上传文件，单文件最大 500 MiB。
- 上传使用持久化公共队列，队列最多容纳 10 个未结束任务，所有管理员均可查看任务申请人、排队位置、传输进度和处理阶段。公共队列只展示仍在排队、传输或后台处理的任务；完成、驳回、失败、取消和过期任务不得继续显示。
- 管理员先为本页面持有的文件申请任务；只有队列有空位时申请才会成功。具体传输开始时间由服务器在并行处理位置空闲时决定，并行数由配置控制。
- 排队、等待传输或传输中的任务依赖页面心跳。页面关闭或失去心跳后任务过期；正在传输的请求立即取消，其 `./tmp` 临时文件必须删除。
- 完整请求体接收完毕后，校验、规范化和迁移转为服务器后台任务，不再因页面关闭而中断。
- 管理员可扫描配置中明确允许的服务器目录。
- 支持 MP3、FLAC、M4A/AAC、WAV 和 OGG。
- 文件扩展名不是信任依据；导入前必须使用 ffprobe 验证确实存在可解码音频流。
- `streaming.output.sample_rate`、`channels` 和 `sample_bits` 同时是最终播放参数与入库上限，默认分别为 48 kHz、2 声道和 32 bit。
- 任一采样参数超过上述上限时，必须先用 FFmpeg 重新采样为符合限制的 FLAC；符合上限的原文件保持原格式。入库后只保留最终合规文件。
- 上传文件先写入 `uploads.temp_dir` 下的任务临时目录。验证与必要的规范化完成后，按存储优先级和迁移后磁盘使用率上限选择目标位置。
- 跨文件系统迁移先复制到目标挂载的隐藏临时文件，校验 SHA-256 后在同一文件系统内原子发布；失败时不得留下可播放的半成品。
- SQLite 只保存存储位置 ID 和系统生成的相对文件名。播放、扫描、删除和修复均先查询曲目记录，再通过当前配置解析实际绝对路径。
- 存储文件名使用系统生成的标识，原始文件名仅作为元数据保存。

### 8.2 元数据

- 自动提取标题、艺人、专辑、时长和内嵌封面。
- 管理员可以编辑文本元数据并上传替换封面。
- 缺少标题时使用清理后的原始文件名，缺少艺人时显示“未知艺人”。
- 使用最终保留文件的 SHA-256 检测精确重复；完全相同的内容自动驳回且不新增媒体记录。上传预约还需检查规范化后的文件名、曲名与艺人组合，发现相似记录时列出候选并要求管理员二次确认，相似名称本身不得触发自动驳回。
- 扫描后消失的文件标记为不可用，不自动删除数据库记录或歌单项。

## 9. 用户界面

### 9.1 听众界面

- 采用深色电台控制台视觉风格，并针对桌面和手机布局分别适配。
- 首屏突出当前频道、直播状态、封面、标题、艺人和服务器播放进度。
- 展示频道完整歌单并高亮当前歌曲，但不为普通用户渲染排序、删除、切歌或“立即播放”控件。
- 提供频道选择、播放/暂停、静音和音量控制。
- 明确显示连接中、缓冲、频道离线、会话失效和网络重连状态。
- 外部播放器面板只显示凭据创建时间、计算出的最晚连接时间、格式选择与操作按钮；不直接显示密钥。用户选择频道后，显式点击复制才临时获取完整地址。刷新有效日期只局部更新该面板，不刷新整页。

### 9.2 管理界面

- 用户审批与状态管理，并展示当前收听在线状态、频道和在线人数。
- 频道创建、编辑、启停和健康状态。
- 音乐上传、目录扫描、元数据与封面编辑。
- 歌单增删、拖动排序、播放模式设置、跳过和立即播放；添加曲目使用可输入筛选的选择器，并从候选中排除当前歌单已有曲目。管理界面还需提供批量添加浮层，支持搜索、多选和全选当前筛选结果，并由后端在单个事务中按候选顺序追加。
- 危险操作需要确认，后台接口仍需独立校验管理员角色，不能只依赖隐藏按钮。

### 9.3 可访问性

- 所有操作可通过键盘完成，并有可见焦点样式。
- 图标按钮提供可读标签；颜色不是状态的唯一表达方式。
- 尊重 `prefers-reduced-motion`。

## 10. 数据模型基线

首版至少包含以下实体：

- `users`：用户名、规范化邮箱、密码哈希、角色、审批状态和时间戳。
- `users` 的外部播放器凭据为可空的 `player_key_created_at` 与 16 字节 `player_key_generation` 单值状态；不得保存完整密钥、密钥哈希或允许同一用户保留多个有效密钥的独立令牌集合。
- `sessions`：会话令牌哈希、用户、创建时间、过期时间和撤销时间。
- `channels`：名称、唯一 slug、说明、启用状态、播放模式和显示顺序。
- `tracks`：逻辑存储位置 ID、相对文件名、原始文件名、哈希、采样参数、规范化状态、元数据、时长、封面和可用状态。
- `upload_jobs`：任务申请人和页面标识、文件名与大小、排队/传输/处理状态、进度、临时文件、最终曲目和错误信息。
- `playlist_items`：频道、歌曲、排序位置和添加时间。
- `playback_states`：时间线锚点、当前项、偏移、随机轮次、歌单版本和进程状态。
- `playback_history`：频道、歌曲、实际开始时间、结束时间和结束原因；每个频道只保留最近 10 条记录。
- `audit_events`：管理员、操作类型、目标、时间和不含秘密的结构化详情。

所有时间在数据库中使用 UTC，界面按配置时区显示。删除用户或频道时应明确处理关联数据，不能依赖 SQLite 默认级联行为。

## 11. API 边界

具体路径可在实现时细化，但必须覆盖以下能力：

- 初始化：查询初始化状态、提交首个管理员。
- 认证：注册、登录、退出、当前用户、CSRF 令牌。
- 播放密钥：登录用户查询当前配置状态，以及通过 CSRF 保护的重新生成操作。
- 听众：频道列表、频道详情、完整歌单、实时状态事件。
- 流授权：供 Nginx `auth_request` 使用的内部只读接口。
- 管理员：用户审批与状态、频道 CRUD、公共上传队列的预约/心跳/传输/取消/事件、目录扫描、元数据编辑、歌单维护和播放控制。

API 错误使用稳定的机器可读错误码；面向用户的文本由前端本地化。上传、扫描和控制操作需要幂等或防重复提交机制。

外部播放器接口为：

- `GET /api/auth/player-key`：登录用户读取是否已配置、创建时间、最晚连接时间和可用格式，不返回密钥或播放地址。
- `POST /api/auth/player-key/regenerate`：登录用户首次生成或替换唯一播放密钥，要求有效 CSRF。
- `POST /api/auth/player-key/url`：用户显式复制时按频道和格式临时返回一个完整连接地址，要求有效 CSRF。
- `GET /listen/aac/{player_key}/{channel_slug}`：持续返回共享 AAC ADTS 音频。
- `GET /listen/flac/{player_key}/{channel_slug}`：仅管理员可用，持续返回共享原生 FLAC 音频。

`/listen` 不使用常规 API 错误结构；其空 `404`/`503` 行为优先于本节前述的机器可读错误约定。

## 12. 配置与秘密

- 非秘密运行参数来自 `config.yaml`，结构以 `config.example.yaml` 为基线。
- 支持通过 `${ENV_VAR}` 引用环境变量，并在启动时验证必填项。
- 密钥、密码和初始化令牌不得提交到版本库。
- 相对路径以配置文件所在目录为基准解析。
- 数据目录、上传临时目录、一个或多个媒体存储根、HLS 运行目录和日志目录必须可分别配置。
- 每个媒体存储根具有稳定唯一 ID、优先级、磁盘使用率上限、启用状态和是否允许自动创建目录的设置。
- 应用启动时进行完整配置校验；未知字段默认视为错误，避免拼写错误被静默忽略。

## 13. 生产部署基线

- 目标为受支持的 Linux 发行版，安装 Python、FFmpeg、Nginx 和 systemd。
- FastAPI 监听 `0.0.0.0:8000`，供域名上游或反向代理连接；生产公网入口仍必须提供 HTTPS，并应通过安全组或防火墙限制 8000 端口的来源。
- 生产环境只提供 HTTPS，并将 HTTP 重定向到 HTTPS。
- Nginx 提供 Vue 静态资源和受保护 HLS，反向代理 `/api`、SSE 与 `/listen` 持续音频流。
- SSE 位置关闭代理缓冲，HLS 分片采用短期缓存策略，HLS 清单不得被长时间缓存。
- SQLite、媒体文件和管理员替换封面需要定期备份；临时 HLS 分片不备份。
- 服务账号采用最小权限，不以 root 身份运行应用或 FFmpeg。
- 日志需要轮转，并避免记录查询字符串中的敏感信息。
- Nginx 与 Uvicorn 均不得记录含路径播放密钥的 `/listen` 请求；明文 HTTP 下的 `/listen` 不重定向，只返回空 `404`。

## 14. 首版明确不包含

- 普通用户上传、点歌、投票切歌或修改歌单。
- 点播播放、任意进度拖动、节目回听和离线下载。
- 交叉淡化、响度标准化管理界面和多档自适应码率。
- 邮箱验证、邮件通知和用户自助找回密码。
- 原生移动应用、WebRTC、Icecast、CDN 和多服务器集群。
- 歌曲版权管理；部署者负责确保拥有合法的存储与传播权限。

## 15. 验收标准

1. 空数据库首次启动时，只有持有一次性初始化令牌的人能够创建首个管理员。
2. 新用户注册后不能登录；管理员批准后可以登录。停用后 API 会话立即失效，HLS 访问最迟在 5 秒授权缓存到期后失效。
3. 未登录请求无法读取 HLS 清单或分片，即使知道完整 URL。
4. 已启用且歌单非空的频道在无人收听时仍持续推进。
5. 两个客户端先后进入同一频道时播放同一首歌，正常网络下进度差不超过两个分片。
6. 普通用户能查看完整歌单，但所有服务端播放和内容管理接口均拒绝其操作。
7. 管理员可看到共享的 10 位上传队列；服务器按配置并行传输，关闭上传页面会过期并清理未完成任务。
8. 上传预约先检查相似名称并要求管理员确认；上传或扫描受支持音频后，系统验证文件、提取元数据并以 SHA-256 自动驳回精确重复。超出 48 kHz、2 声道或 32 bit 的音频规范化为 FLAC 后才写入合适的存储挂载。
9. 管理员切歌后，所有新旧客户端在合理的 HLS 缓冲时间内进入同一首新歌曲。
10. 服务中断并重启后，频道按持久化时间线恢复到预期歌曲和近似位置。
11. 单机同时运行 5 个频道、50 个听众时，不出现持续增长的 FFmpeg 进程、HLS 文件或内存泄漏。
12. 桌面和手机浏览器均可完成注册、登录、选台和收听；浏览器禁止自动播放时有清晰提示。
13. 生产部署通过 HTTPS 反向代理提供服务，FastAPI 的 8000 端口只接受预期上游流量，HLS 运行目录不直接暴露到公网。
14. 管理员用户列表能在一个收听活动超时窗口内显示实际获取 HLS 的用户及频道，暂停接收后自动转为离线，重复分片请求不会重复计数。
15. 外部播放器凭据无效时只能观察到空响应体 `404`；无法从响应、访问日志或错误日志中获知拒绝原因或完整密钥。
16. 同一频道的多个 AAC 外部连接与浏览器 HLS 共用一个 AAC 编码器；单个慢连接最多积压约 5 秒，且新的同密钥连接会在 10 秒内完全取代旧连接。

## 16. 实现顺序

1. 建立项目骨架、配置校验、数据库迁移和安全的首次初始化流程。
2. 实现账号注册、审批、登录、会话、CSRF 和角色权限。
3. 实现音乐导入、ffprobe 验证、元数据提取和歌单管理。
4. 实现单频道监督器、时间线、FFmpeg HLS 和重启恢复。
5. 扩展到多频道并实现管理播放控制。
6. 实现 Vue 听众界面、hls.js 播放和 SSE 状态同步。
7. 实现完整管理后台。
8. 增加 Nginx `auth_request`、systemd、HTTPS 部署文件和容量验证。
